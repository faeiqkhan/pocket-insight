# Example environment variables for Pocket Insight
# Copy this file to `.env.local` or `.env` and fill in real values.
# Vite only exposes variables prefixed with VITE_ to the client.

# Supabase project URL (example: https://xyzabc.supabase.co)
VITE_SUPABASE_URL=

# Supabase anon/public key (client-side)
VITE_SUPABASE_ANON_KEY=

# Optional: app metadata
VITE_APP_TITLE=Pocket Insight

# NOTE: Do NOT store server-only secrets (service_role keys, private keys) in client-exposed env files.
# Keep any server-side secrets in your deployment platform's secret store or on a backend server.
